name: Windows

on:
  workflow_dispatch:

jobs:
  windows-pixi-status:
    runs-on: windows-latest

    steps:
      - run: |
          echo "Windows CI is paused until pixi.toml defines and validates a win-64 environment."
          echo "Current active Pixi platforms are linux-64 and osx-arm64."
